h1
  span(style="color: #ffbcbc;") O
  span(style="color: #ff91f5;") C
if input[1] !=  "?list=true"
  header too slow? <a id="footer-link" href="./oc?list=true">check out speedy mode.</a>
  each val,key in input[0]
    time= val.split('.')[0]
    img(src='https://files.1ctinus.me/oc/' + val)
else
  ul
    each val,key in input[0]
      li: a(href='https://files.1ctinus.me/oc/' + val)= val.split('.')[0]
  footer too boring? <a id="footer-link" href="./oc">check out normal mode.</a>