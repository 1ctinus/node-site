body
head
    //- style.
    //-     body {
    //-     outline: 10px solid red;
    //-     }
    link#css(rel='stylesheet', href='/css/questions.css')
    style.
        html {scrollbar-color:  rgba(0, 0, 0, 0.5) transparent;}
    meta(charset='UTF-8')
    meta(name='author', content='1ctinus')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='description', content='the bestest webzone on the internets')
    meta(data-react-helmet='true', name='theme-color', content='#ff00ff')
    meta(content='https://1ctinus.me/icon.png', name='twitter:image')
    meta(content='https://1ctinus.me/icon.png', property='og:image')
    meta(content='https://1ctinus.me/icon.png', property='og:image:secure_url')
form(action = "/request", method = "POST")
  input(name = "question" preview = "An interesting title...")
  button(type = "submit") Ask
- var title = JSON.parse(input)
each val, key in title.questions
  h2
    span.name= val.time
    | 
    span.date= val.question
  p= val.answer
//- p This is #{JSON.parse(input).questions[1].time}